echo Never trust, always verify. > data.txt
type data.txt
openssl dgst -sha256 data.txt

openssl dgst -sha256 -hmac "secretkey123" data.txt

echo Never trust, always verifiy. > data.txt
openssl dgst -sha256 -hmac "secretkey123" data.txt

